const Err = require('./../config/error')
const Say = require('./../config/message')

const User = require('./../models/User').model
const User = require('./../models/Race').model
const User = require('./../models/Team').model
const User = require('./../models/Result').model

module.exports = new Class Memberizer {
    // leave team as a member
    function memberLeaveTeam (user, team) {
        
    }

    // join team
    function memberJoinTeam (user, team) {

    }
    
    // transfer ownership
    function transfer (user, team) {
        
    }
}

// what about when someone removes the last member from a team?